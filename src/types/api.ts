enum FileType {
  CV = "CV",
}

enum EntityType {
  USER = "USER",
  RESEARCHER = "RESEARCHER",
  ORGANISATION = "ORGANISATION",
  CUSTODIAN = "CUSTODIAN",
  ADMIN = "ADMIN",
}

export { EntityType, FileType };
